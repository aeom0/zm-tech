{
  "name": "geemastudio-server",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'geemastudio-server: ops/DB — usa pnpm db:push o supabase functions deploy'",
    "check:types": "tsc --noEmit",
    "db:push": "drizzle-kit push",
    "db:generate": "drizzle-kit generate",
    "db:studio": "drizzle-kit studio"
  },
  "dependencies": {
    "@geemastudio/shared-schema": "workspace:*",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "drizzle-kit": "^0.31.4",
    "tsx": "^4.19.4",
    "typescript": "~6.0.3"
  }
}
