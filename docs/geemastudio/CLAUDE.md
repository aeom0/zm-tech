# CLAUDE.md — GeemaStudio

@AGENTS.md

## Claude Code

- Skills/rules viven en la raíz del monorepo: `.cursor/skills/`, `.cursor/rules/`, `.cursorrules`.
- Claude: `.claude/skills` → symlink a `.cursor/skills` (skill `geemastudio-dev`).
- Paths en `AGENTS.md` con `apps/mobile` / `apps/web` = `apps/geemastudio-mobile` / `apps/geemastudio-web` en zm-tech.
