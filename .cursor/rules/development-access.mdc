---
description: Acceso a archivos de desarrollo y .env para depuración (GeemaStudio y monorepo)
globs:
  - "**/.env"
  - "**/.env.local"
  - "**/.env.development"
  - "**/.env.example"
  - "**/config/**"
alwaysApply: true
---

# Development Access — zm-tech

## Permisos para desarrollo

- Tienes acceso completo a archivos de desarrollo, incluido `.env` y `.env.local`, para depurar conexión a Supabase, variables de Expo y Next.js.
- Los archivos `.env` son para desarrollo local; no incluir secretos de producción en el repo (`.env` está en `.gitignore`).
- Ayudar a resolver errores de `EXPO_PUBLIC_SUPABASE_*` / `NEXT_PUBLIC_SUPABASE_*` y arranque de mobile/web en local.

## Contexto multi-proyecto

Mapa canónico: [docs/SUPABASE.md](../../docs/SUPABASE.md).

- **GeemaStudio**: `udelxwwnyivknslueerr` — mobile/web → Supabase directo. `geemastudio-server` = ops/DB (Drizzle/seeds/Edge), no API JWT.
- **Landing + OdentalPro + RepMAX**: hub `llacowjutjfefboqgfnj` — prefijos/tablas por producto; no mezclar.

## Seguridad

- Secretos de producción no deben estar en el repo; en despliegue usar variables de entorno del host.
- En desarrollo local, leer `.env` para diagnosticar problemas está permitido y es útil.
